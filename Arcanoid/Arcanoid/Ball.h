/* header file for the Ball. */

#ifndef __BALL_H__
#define __BALL_H__

/*
#include "Oblong.h"
#include "Sprite.h"

class Ball : public Oblong, public Sprite {
private:
	bool isActive;

public:
	Ball(int start_x, int start_y);
	void Activate();
	void Deativate();
	bool isActive();
	~Ball();
};

*/
#endif